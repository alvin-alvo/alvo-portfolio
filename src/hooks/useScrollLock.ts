'use client';

import { useEffect } from 'react';

export function useScrollLock() {
    useEffect(() => {
        // Scroll lock logic to be implemented later if needed.
        // Currently handled by CSS overflow: hidden in layout.
    }, []);
}
